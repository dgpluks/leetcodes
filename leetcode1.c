#include<stdio.h>
int main(){
	int qn, numeros;
	scanf("%d", &qn);
	for(int i = 0; i< qn; i++){
		scanf("%d", &numeros[i]);
	}
}
int* twoSum(int* nums, int numsSize, int target, int* returnSize) {
	struct no = malloc(sizeof(struct lista));
}
struct no{
	float valor;
	int *proximo;
}
